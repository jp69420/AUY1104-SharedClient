apiVersion: v1
kind: Service
metadata:
  name: demo-api
spec:
  selector:
    app: demo-api
    color: ${ACTIVE_COLOR}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30090
  type: NodePort